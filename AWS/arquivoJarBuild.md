***BUILDAR A APLICAÇÂO, GERAR O ARQ JAR***

**Comandos:**
```mvn clean package```
ou
```mvn clean install -DskipTests```
ou
```./mvnw clean install -DskipTests```

O arquivo fica dentro da pasta target.